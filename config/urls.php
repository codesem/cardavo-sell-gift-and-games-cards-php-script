<?php

/**
 * URL to the Assets directory.
 */
define('AST', 'template' . DS . Theme . DS . 'Assets' . DS);

/**
 * URL to the Theme directory.
 */
define('THM', 'template' . DS . Theme . DS);

/**
 * URL to the Upload directory.
 */
define('UPD', 'template' . DS . 'uploads' . DS);

/**
 *
 * URL to the Admin directory.
 */
define('ADM', 'admin' . DS);

/**
 *
 * URL to the User directory.
 */
define('USR', 'user' . DS);

/**
 *
 * URL to the Category directory.
 */
define('CAT', 'category' . DS);

/**
 *
 * URL to the Item directory.
 */
define('PRD', 'product' . DS);